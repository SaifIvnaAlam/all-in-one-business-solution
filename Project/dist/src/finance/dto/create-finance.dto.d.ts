export declare class FinanceDTO {
    id: number;
    date: Date;
    expenses: number;
    revenue: number;
    total: number;
    description: string;
    constructor(id: number, date: Date, expenses: number, revenue: number, total: number, description: string);
}
